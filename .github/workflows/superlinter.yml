name: Super-Linter

on: push

jobs:
  super-lint:
    name: super-eslinter 
    # The type of machine to run the job on
    runs-on: ubuntu-latest

   steps:
      - name: Run linter
        # Run Linter
        run: make lint
